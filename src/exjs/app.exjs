defmodule App do
  alias Components.App, as: ReactApp
  alias Stores.Store
  
  JS.import [createStore], "redux"


  def main() do
    store = createStore(Store["graphics"])
    JS.update(window, "store", store)
    ReactApp.start()
  end

  main()

end
